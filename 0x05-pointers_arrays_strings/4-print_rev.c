#include "main.h"
/**
 * print_rev - prints a string, in reverse, followed by a new line.
 * @s: input string.
 * Return: void.
 */
void print_rev(char *s)
{
	int lenght;

	while (lenght != '\0')
	{
		lenght++;
	}
	i--;

	while (lenght >= 0)
	{
		_putchar(s[lenght]);
		i--;
	}
}
